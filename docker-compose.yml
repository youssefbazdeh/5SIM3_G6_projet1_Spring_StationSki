version: '3'

services:
  mysqldb:
    image: arm64v8/mysql:8.0.34
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=stationSki
    ports:
      - 3306:3306
    volumes:
      - db:/var/lib/mysql
volumes:
  db:

